# Shortcuts
| symbol | Description |
| ------ | ----------- |
| `!!`   | Expands to the last command run |
| `!!:*` | Expands to the arguments of the last command run |
| `^foo^bar` | runs the last command after replacing occurrences of "foo" with "bar". Useful for correcting typos. May be shell-specific. |
